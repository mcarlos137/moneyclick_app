import React from 'react';
import { View, Text } from 'react-native';
//import { navigateStore } from '../../main/store'

const FiatMerchantPoSScreen = ({ navigation, route }) => {
  return (
    <View style={{
      flex: 1
    }}>
      <Text>{'FIAT MERCHANT POS'}</Text>
    </View>
  );
};

export default FiatMerchantPoSScreen;
